import styled from 'styled-components'

import { IconButton } from '@material-ui/core';

export const MenuButton = styled(IconButton)`
    color: var(--drawer-menu-button);
    margin-right: 1rem;     
`